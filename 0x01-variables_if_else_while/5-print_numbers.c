#include <stdio.h>
/**
 * main - main entry point for the program
 * Return: returns 0 in case of success
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
