#include <stdio.h>
/**
 * main - main entry point for the program
 * Return: returns 0 in case of succes
 */
int main(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
